{
  "name": "root",
  "npmClient": "yarn",
  "private": true,
  "dependencies": {},
  "scripts": {
    "build": "lerna run build --stream"
  },
  "devDependencies": {
    "typescript": "^3.6.3"
  }
}
